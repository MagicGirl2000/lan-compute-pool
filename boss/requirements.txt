flask>=3.0
psutil>=5.9
requests>=2.28
numpy>=1.24
# 可选 / optional：
#   pynvml        —— NVIDIA GPU 利用率/显存读取（无则自动退回 nvidia-smi 或跳过）
#   llama-cpp-python —— 本地模型项目用 llama_cpp 后端跑真实 gguf 模型
#   transformers + torch —— 本地模型项目用 transformers 后端（GPU 自动启用）
